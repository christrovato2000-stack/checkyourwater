NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=

# Shared secret for POST /api/revalidate. Any random string works — set
# the same value in Vercel env vars. Used to flush the ISR cache after a
# manual data refresh.
REVALIDATION_SECRET=
